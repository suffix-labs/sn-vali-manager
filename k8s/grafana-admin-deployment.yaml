apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana-admin
  labels:
    app: grafana-admin
    grafana-tier: admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana-admin
  template:
    metadata:
      labels:
        app: grafana-admin
        grafana-tier: admin
    spec:
      containers:
      - name: grafana
        image: grafana/grafana:latest
        ports:
        - containerPort: 3000
        env:
        - name: GF_SECURITY_ADMIN_PASSWORD
          value: "admin123"
        - name: GF_USERS_ALLOW_SIGN_UP
          value: "false"
        - name: GF_AUTH_ANONYMOUS_ENABLED
          value: "false"
        volumeMounts:
        - name: grafana-storage
          mountPath: /var/lib/grafana
        - name: grafana-datasources
          mountPath: /etc/grafana/provisioning/datasources
          readOnly: true
        - name: grafana-dashboards-config
          mountPath: /etc/grafana/provisioning/dashboards
          readOnly: true
        - name: grafana-admin-dashboards
          mountPath: /var/lib/grafana/dashboards
          readOnly: true
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      volumes:
      - name: grafana-storage
        persistentVolumeClaim:
          claimName: grafana-admin-pvc
      - name: grafana-datasources
        configMap:
          name: grafana-admin-datasources
      - name: grafana-dashboards-config
        configMap:
          name: grafana-dashboards-config
      - name: grafana-admin-dashboards
        configMap:
          name: grafana-admin-dashboards
---
apiVersion: v1
kind: Service
metadata:
  name: grafana-admin-service
  labels:
    app: grafana-admin
    grafana-tier: admin
spec:
  selector:
    app: grafana-admin
  ports:
  - port: 3000
    targetPort: 3000
  type: LoadBalancer
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-admin-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi