apiVersion: apps/v1
kind: Deployment
metadata:
  name: suffix-validator
  labels:
    app: suffix-validator
    validator: suffix-validator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: suffix-validator
  template:
    metadata:
      labels:
        app: suffix-validator
        validator: suffix-validator
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: suffix-validator-attestation
        image: ghcr.io/eqlabs/starknet-validator-attestation:latest
        ports:
        - containerPort: 9090
          name: metrics
        env:
        - name: RUST_LOG
          value: "info"
        args:
        - "--metrics-address"
        - "0.0.0.0:9090"
        - "--local-signer"
        - "--node-url"
        - "http://pathfinder-service:9545/rpc/v0_8"
        - "--staker-operational-address"
        - "$(STAKER_OPERATIONAL_ADDRESS)"
        envFrom:
        - secretRef:
            name: suffix-validator-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: suffix-validator-service
  labels:
    app: suffix-validator
    validator: suffix-validator
spec:
  selector:
    app: suffix-validator
  ports:
  - name: metrics
    port: 9090
    targetPort: 9090
  type: ClusterIP
